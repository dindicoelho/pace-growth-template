# Staging Environment
NODE_ENV=staging
NEXT_PUBLIC_APP_URL=https://staging.yourapp.com
NEXT_PUBLIC_APP_ENV=staging

# Use staging database
DATABASE_URL=postgresql://user:password@staging-db:5432/dbname

# Use staging services
# ... (mesma estrutura do .env.example mas com valores de staging)